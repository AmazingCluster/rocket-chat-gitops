apiVersion: v1
kind: Service
metadata:
  name: {{ include "rocket-chat.fullname" . }}
  labels:
    {{- include "rocket-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rocket-chat.selectorLabels" . | nindent 4 }}
